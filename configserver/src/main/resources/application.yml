server:
  port: 8762
spring:
  application:
    name: configserver  #配置中心
  profiles:
    active: native  #本地配置
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared